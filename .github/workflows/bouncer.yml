name: Commit Prefix Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 

      - name: Check Commit Message
        uses: gsactions/commit-message-checker@v2
        with:
          pattern: '^(patch|feature|update): .*'
          error: 'STOP! Your commit must start with "patch:", "feature:", or "update:".'
          checkAllCommits: 'true'

      - name: Auto-merge if Bouncer passes
        uses: "pascalgn/automerge-action@v0.16.4"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          MERGE_METHOD: "squash"
          MERGE_LABELS: ""          # <--- Keeping this empty means "Merge everything"
          MERGE_FILTER_AUTHOR: ""   # <--- Ensures it doesn't ignore you
          MERGE_FORKS: "false"
          MERGE_RETRIES: "10"       # <--- Gives it more timee
          MERGE_RETRY_SLEEP: "10000"